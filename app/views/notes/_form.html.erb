<%= form_with(model: @note, url: new_or_confirm, local: true) do |f| %>
	<%= render 'error_messages' %>
	<div class="note_content">
		<%= f.label :content %>
		<%= f.text_field :content %>
	</div>
	<%= f.submit %>
<% end %>