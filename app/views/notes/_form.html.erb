<%= form_with(model: @note, local: true) do |f| %>
	<%= render 'error_messages' %>
	<div class="note_content">
		<%= f.label :content %>
		<%= f.text_field :content %>
	</div>
	<%= f.submit %>
<% end %>